import Grub from './Grub'

class Bee extends Grub {
  constructor(age, color, job) {
    super()
  	this.age = 5;
  	this.color = 'yellow';
  	this.job = 'Keep on growing';
  }
};

export default Bee;